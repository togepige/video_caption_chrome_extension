var MenuWindow = require('./components/menu.react.jsx');
var React = require('react');
var ReactDOM = require('react-dom');

// Render the menu window component
ReactDOM.render(
    <MenuWindow />,
    document.getElementById('application')
);
